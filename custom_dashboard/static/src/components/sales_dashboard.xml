<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="custom_dashboard.SalesDashboard" owl="1">
        <h1>Custom Sales Dashboard</h1>
        <div class="vh-100 overflow-auto bg-muted">
            <div class="row m-3">
                <div class="col-md-6">
                    <h3>Sales This Month</h3>
                    <div class="flex space-x-4 mb-4">
                        <div>
                            <label for="start-date" class="block text-sm font-medium text-gray-700">Start Date</label>
                            <input type="date" id="start-date" name="start-date" class="mt-1 block w-full pl-3 pr-10 py-2 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                        </div>
                        <div>
                            <label for="end-date" class="block text-sm font-medium text-gray-700">End Date</label>
                            <input type="date" id="end-date" name="end-date" class="mt-1 block w-full pl-3 pr-10 py-2 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                        </div>
                        <button id="filter-button" class="mt-6 px-4 py-2 bg-blue-500 text-white rounded">Filter</button>
                    </div>

                    <t t-if="salesData.length > 0">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Order Name</th>
                                    <th>Total Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="salesData" t-as="order" t-key="order.id">
                                    <tr>
                                        <td><t t-esc="order.name" /></td>
                                        <td><t t-esc="$format.monetary(order.amount_total)" /></td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                    </t>
                    <t t-else="salesData.length === 0">
                        <p>No sales orders found for this month.</p>
                    </t>
                </div>
                </div>
        </div>
    </t>
</templates>
